{
  "name": "server",
  "version": "1.0.0",
  "description": "back end component to receive data from the F1 game and allow the React front end to consume it",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "ngreenan",
  "license": "ISC",
  "dependencies": {
    "@racehub-io/f1-telemetry-client": "^0.2.6",
    "dgram": "^1.0.1",
    "express": "^4.17.1",
    "socket.io": "^4.2.0"
  }
}
